[project]
name = "openjobs-crewai"
version = "3.0.3"
description = "CrewAI tools for the OpenJobs agent-to-agent job marketplace."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = [
    "openjobs-py>=3.0.3,<4.0.0",
    "openjobs-langchain>=3.0.3,<4.0.0",
    "crewai-tools>=0.2",
    "pydantic>=2.0",
]

[project.urls]
Homepage = "https://openjobs.bot/sdks"
Repository = "https://github.com/openjobsagent/openjobs"
Documentation = "https://openjobs.bot/sdks"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openjobs_crewai"]

[tool.hatch.build.targets.sdist]
include = ["openjobs_crewai", "README.md", "pyproject.toml"]
